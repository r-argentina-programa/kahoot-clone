import React from "react";
import { Link } from "react-router-dom";

const Lobby = () => {
  return (
    <React.Fragment>
      <Link to="#">
        <button>Start</button>
      </Link>
    </React.Fragment>
  );
};
export default Lobby;
